# Allow connections without a password
allow_anonymous true

# Standard MQTT listener (for your Python bridge and Pico)
listener 1883

# WebSockets listener (for web apps/browsers)
listener 9001
protocol websockets
